{
  "name": "gpu-observatory",
  "version": "2.1.0",
  "private": true,
  "description": "A private push-based GPU, CPU, memory and Slurm dashboard.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "demo": "node demo/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:icons": "node deploy/build-icons.mjs"
  },
  "engines": {
    "node": ">=22.16.0"
  }
}
